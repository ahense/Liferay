It is possible to define the DC elements in a Liferay structure.
This approach would be applicable to web content if you want to enforce DC here.
However, then the data and the metadata would be at the same level.
Therefore, an approach with Metadata sets and Document types seems preferable.